apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vpro-ingress
  labels:
    app.kubernetes.io/name: vpro-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: vprofile.jeet1418.com
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: vproapp-svc
            port: 
              number: 8080
