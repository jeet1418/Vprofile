apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vprofile-charts.fullname" . }}-test-connection"
  labels:
    {{- include "vprofile-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vprofile-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
