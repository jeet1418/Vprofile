apiVersion: v1
kind: Service
metadata:
  name: {{ include "vprofile-charts.fullname" . }}
  labels:
    {{- include "vprofile-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vprofile-charts.selectorLabels" . | nindent 4 }}
