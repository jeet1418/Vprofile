apiVersion: v1
kind: Service
metadata:
  name: vpromq01
spec:
  selector:
    app: vprormq
  ports:
  - port: {{ .Values.rabbitmq.service.port }}
    targetPort: vprormq-port
    protocol: TCP
  type: {{ .Values.rabbitmq.service.type }}
